program test1;
var i: boolean;
	c: char;
	d: integer;
begin
  writeln("Hello World");
  i := not false and true;
  writeln(1 + 2);
  
  for c := 'a' 'z' do
	writeln(c));
	
  for d := 1 to 3 do
	writeln(c);
end.